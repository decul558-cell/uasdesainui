[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader --ignore-platform-reqs --no-scripts --no-interaction"]

[phases.build]
cmds = []

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
